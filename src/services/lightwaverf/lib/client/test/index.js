import { expect } from 'chai';
import sinon from 'sinon';
import nock from 'nock';
import WebSocket from 'ws';
import LightwaveRfApi from '../src';

describe('LightwaveRfApi', () => {
  beforeEach(() => {
    sinon.createStubInstance(WebSocket, {

    });
  });

  describe('#authenticate', () => {
    it('should
  });

  afterEach(() => {
    sinon.restore();
  });
});
